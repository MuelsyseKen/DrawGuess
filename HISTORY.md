# HISTORY —— 开发历史

本文件只增不减。每次有架构性决定、踩坑、放弃的方案，都往下追加，不要删除旧记录。

---

## 2026-09-23 —— 项目启动，第一轮需求澄清

- 用户提出做一个本地部署、网页可访问的"你猜我画"多人游戏，分竞猜/接龙两种模式，署名 `Muelsyse & long_ken & Claude`。
- 第一次对话按用户要求，先不写代码，只做需求澄清，确认后再产出 README.md / Agents.md / FULLREADME.md / HISTORY.md 四份文档。
- 澄清过程中确认的关键决策：
  - **技术栈**：Node.js + Express + Socket.io（后端）+ Vue3（前端）+ SQLite（用户/战绩/作画记录持久化）+ 内存（房间/对局状态，重启清空）。用户明确表示"目前也没打算做大"，所以没有选更重的数据库方案。
  - **Unity 转型**：用户最初描述里提到"支持转型到 Unity"，容易被理解为需要现在就做引擎无关的架构解耦。经确认，用户的真实意图是方案 (A)——只要求协议（房间状态/笔迹/词库/计分）设计清晰，不要求代码层面做额外抽象。用户还提到"网页版做完后大概率就当甩手掌柜，不会真的做 Unity 版"，所以这条只作为"协议设计规范"而非"架构约束"来落地，避免过度设计。
  - **画板笔迹存储**：确认必须按矢量序列（坐标+颜色+粗细+时间戳）存储，而不是位图截图，为将来的"作画回放"功能预留空间。一期是否做回放播放器 UI 待定，但数据结构从一开始就要对。
  - **房间人数上限**：不写死具体数字，设计上要能支持到 32 人（截图里竞猜 2-16、接龙 4-16 只是示例默认值，不是硬上限）。
  - **匿名投票**：确认只隐藏投票人身份，投票结果依然公开显示，不是"匿名统计不公开结果"。
  - **词库格式**：确认为"题库目录下按分类存 txt 文件，每行一个词"的方案。
  - **用户系统范围扩大**：最初描述只提到登录/登出，确认后追加了战绩/历史记录/排行榜、以及玩家历史作画记录（用户原话"黑历史"）三项持久化需求，因此数据库表设计里加入了 `game_records` 和 `drawings` 两张表。
  - **响应式适配**：用户额外强调"一定要做好不同设备的分辨率适配"，手机端和电脑端、不同浏览器窗口都要考虑，这条被记入 `Agents.md` 的硬性要求，避免后续开发图省事只做桌面端。
- 本轮没有产出任何代码，只产出四份基础文档。下一步应从项目骨架初始化 + 登录注册模块开始。

---

## 2026-09-23 —— 引入 Phase 划分与 Git 分支工作流

- 用户提出：为了避免多个对话/多个 AI 同时改动导致混乱，希望引入"Phase（阶段）"概念——每个 Phase 对应一次对话、一条 GitHub 分支，做完确认无误后再合并到 `main`。参考的是 `muelsyseken-launcher` 项目已经在用的分支模式。
- 据此把整个开发拆成 7 个 Phase（详见 `FULLREADME.md` 第9节）：骨架+账号 → 房间系统 → 画板引擎 → 竞猜模式 → 接龙模式 → 战绩/排行榜 → 响应式打磨+部署收尾。如果后续发现某个 Phase 范围太大，可以在开工前再拆子 Phase，不需要回来改这份文档的编号体系。
- 用户提到之后会提供一个**仅限本仓库**的细粒度 GitHub Token，权限只有 **Contents** 和 **Pull Requests**。据此在 `Agents.md` 里加了一条硬性规则：**AI 不自行合并 PR**，必须等用户明确确认后才能合并——即便 Token 权限理论上可能覆盖到合并接口，这条规则也不因为"技术上能做到"而松动。同时明确 Token 没有 Actions/Issues/仓库设置等权限，不要去尝试这些操作。

---

## 2026-09-23 —— Phase 1（骨架 + 账号系统）开发

- 从最新 `main` 拉出 `phase-1-skeleton-auth` 分支，按 `FULLREADME.md` 第9节 Phase 1 的范围开发：项目骨架 + 账号系统。
- **SQLite 驱动选择**：Node 22 自带实验性 `node:sqlite`，本地验证可用，但官方标注"随时可能变动"，风险偏高，改用生态成熟的 `better-sqlite3`；容器里验证过能装预编译二进制，不需要本地编译工具链。
- **鉴权方案**：文档没有明确规定鉴权方式，选择了 JWT + httpOnly cookie（`jsonwebtoken` + `bcryptjs`），没有引入 Redis/session store 等更重的方案，符合 `Agents.md` "不擅自引入重型依赖"的要求；`bcryptjs` 用纯 JS 实现而非 `bcrypt`，同样是为了避免原生编译的不确定性。
- **用户名/密码校验规则**：文档未定义具体规则，本 Phase 自行拍板为"用户名 3~20 位字母/数字/下划线/中文，密码 6~72 位"，记在这里供后续确认；如需调整，改 `backend/src/routes/auth.js` 里的正则和长度常量即可。
- **前端状态管理**：账号状态只用 Vue3 内置的 `reactive`/`readonly` 做了一个轻量 `useAuth()` composable，没有引入 Pinia——Phase 1 只有登录态这一项全局状态，等后续 Phase 状态变复杂了再评估。
- **创建/加入房间入口**：大厅页按 `FULLREADME.md` 第3节把入口 UI 做出来了，但点击后只弹出"Phase 2 实现"的提示，不含任何房间逻辑，避免跨 Phase 写代码。
- **`game_records` / `drawings` 表**：按 `FULLREADME.md` 第6节字段草案一并建了表（避免以后改 `users` 表结构引发迁移问题），本 Phase 没有任何代码读写这两张表。
- 手工验证过：后端全套账号接口（注册/登录/查询当前用户/登出，含用户名重复、密码错误等异常分支）用 curl 跑通；前端 `vite build` 生产构建跑通；前后端本地联调（5173 → 3000）验证过 CORS + cookie 携带正常。没有引入自动化测试框架（Jest/Vitest 等），判断 Phase 1 范围内没有必要，后续如果用户需要可以再补。
---

## 2026-09-23 —— Phase 1 安全审查修复

- 用户拿着 Deepseek 和 Gemini 两份独立审查意见回来，两份意见高度重合（都点出了 `JWT_SECRET` 弱默认值、cookie 属性不一致导致登出失效、Socket.io 握手无鉴权这三个核心问题），交叉验证之后可信度较高，按各自建议逐条评估、大部分直接修了。具体清单和验证记录见 `FULLREADME.md` 第10节"安全问题跟踪"，不在这里重复。
- **补充了一条流程规范**：以后涉及鉴权/账号相关代码的 Phase，收尾前要过一遍安全自查，发现的问题不管修没修都要登记到 `FULLREADME.md` 第10节，写进了 `Agents.md`。这样以后不用每次都靠"用户拿着第三方审查回来"才发现问题。
- **明确没有采纳的建议**：Redis/token 黑名单——两份审查都提到"登出不会让旧 token 失效"，但这是 JWT 无状态特性的固有取舍，引入 Redis 属于 `Agents.md` 明令禁止的"未经确认的重型依赖"，Phase 1 阶段不做，记在跟踪表里作为已知取舍，不是遗漏。
- **改了但两份审查都没提到的一点**：`created_at`/`played_at` 的默认值从 `datetime('now')` 换成了 `strftime` 输出的标准 ISO 8601 格式（带毫秒和 `Z` 后缀）——这条其实是 Deepseek 提的"建议改"项，评估后发现是这批修复里最不涉及连锁改动的一条，顺手一起做了，开发库直接删除重建（Phase 1 还没有需要保留的正式数据）。
- 全部修复项跑了一遍手工验证：注册/登录/me/登出（确认 cookie 真的被清掉、清完之后 `me` 返回 401）、`NODE_ENV=production` 时不设 `JWT_SECRET` 会直接拒绝启动、`Authorization: Bearer` 头不再能单独鉴权、连续 21 次登录请求第 20 次开始触发限流（429）。前端 `vite build` 重新跑过确认没有因为输入框加 `minlength`/`maxlength` 出问题。

---

## 2026-09-23 —— Phase 2（房间系统）开发

- 从最新 `main` 拉出 `phase-2-rooms` 分支，先在 `FULLREADME.md` 新增第11节把房间数据结构、设置字段范围、REST 接口、Socket.io 事件协议写清楚，再动代码（`Agents.md` 第2条"协议优先"）。
- **房间状态纯内存**：`backend/src/rooms/store.js` 用一个 `rooms` Map + 一个 `userId -> 当前房间` 的索引 Map 维护，符合 `FULLREADME.md` 第6.2节的既定方案，没有引入任何持久化。
- **断线重连策略拍板**：用户明确要求"掉线可以重连，但只等 60 秒，超时直接踢出不能重连"。实现上没有走"立刻移出房间"的简单方案，而是断线后先标记 `connected:false` + 启动 60 秒 `setTimeout`，期间同一 `userId`（靠登录态 cookie 识别）重新连接会被自动接回房间；超时才正式移出（房主顺延/房间清空）。宽限时长通过 `DISCONNECT_GRACE_MS_OVERRIDE` 环境变量可覆盖，仅用于测试，生产环境不设置就是默认 60 秒。这条决策记入了 `FULLREADME.md` 第8节和第11.1节。
- **词库读取**：`backend/src/wordbanks/index.js` 按第7节方案读 `backend/wordbanks/*.txt`，本 Phase 顺带放了三个示例分类（动物/日常用品/食物，各10个词）方便联调；正式内容后续按需替换/扩充即可，不影响代码逻辑。
- **房间设置校验**：`backend/src/rooms/validateSettings.js` 严格按第4节的范围表校验，超出范围直接拒绝（`INVALID_SETTINGS`），不做静默 clamp，避免前后端"以为生效了但其实被改了"的心智不一致。特殊效果字段除 `'none'` 外一律拒绝启用（一期只做 UI 占位），呼应第4节"选项禁用即可，不实现效果逻辑"的要求。
- **公开/私人切换的落地位置**：确认放在房间大厅页（`room:setPublic`），创建房间的参数设置页不包含这个开关，`room:create` 一律以 `isPublic:false` 起步——这是照着 `FULLREADME.md` 第3节"房间大厅页……默认私人房间，可在设置里转为公开"的字面意思实现的，没有自己加戏在创建流程里塞一个开关。
- **安全自查**（`Agents.md` 安全审查规范，房间系统涉及"邀请码"这类靠不可猜测性做访问控制的机制，按规定加做一轮）：发现 `room:joinByCode` 没有限流，理论上可以脚本化撞库猜活跃房间的邀请码；修了——手写了一个极简内存滑动窗口限流器（`backend/src/utils/rateLimit.js`，没有为这一个 socket 事件专门加新依赖），同用户每分钟最多 20 次尝试。另外发现两条没有现在修的：①同一用户可以无限制 `room:create` 占内存（暂不做全局配额，本地部署场景风险有限）；②Socket.io payload 没有额外收紧大小限制（吃 Socket.io 自带的 1MB 默认上限兜底）。三条都记进了 `FULLREADME.md` 第10节 Phase 2 审查表。
- **测试方式**：没有引入自动化测试框架（沿用 Phase 1 的"已知取舍"判断），写了一个独立的 Node 脚本（`socket.io-client` + `axios`，不在仓库里，纯本地调试用）跑了一遍完整协议：无效设置拒绝、房间满员拒绝、创建/公开列表/邀请码加入/错误邀请码拒绝、非房主改设置拒绝、设置广播、断线重连（含用短 `DISCONNECT_GRACE_MS_OVERRIDE` 验证"超时真的会被踢出"这条，不只是等宽限期内重连成功）、公开转私人后列表联动、主动离开/房主顺延/房间销毁、非法词库分类拒绝——全部手工验证通过。前端额外跑了 `vite build` 和 `vite dev` 确认没有编译期错误。
- **前端状态管理**：延续 Phase 1 的选择，房间状态也是轻量 `reactive`/`readonly` 的 `useRoom()` composable（`frontend/src/stores/room.js`），订阅 Socket.io 广播事件维护 `players`/`settings`/`isPublic` 等字段，没有引入 Pinia。
- **前端页面**：新增创建房间-模式选择页、创建房间-参数设置页（竞猜/接龙共用一个组件，按 `mode` 动态渲染字段）、加入房间页（私人邀请码 tab + 公开房间列表 tab）、房间大厅页（邀请码展示+复制、玩家列表含"掉线重连中"状态提示、房主可修改设置/切换公开私人）。路由新增了简单的 `requiresAuth` 守卫（未登录访问房间相关路由会跳回大厅），补上了 Phase 1 记录在案的"前端目前无路由守卫"这条已知取舍。
- 房主离开/断线超时的顺延逻辑：直接顺延给 `players` 数组里的下一位（加入顺序），没有做更复杂的"顺延给在线玩家"之类的策略，符合当前范围的简单实现原则。

---

## 2026-09-24 —— Phase 3（画板引擎）开发

- 从最新 `main` 拉出 `phase-3-canvas-engine` 分支，先在 `FULLREADME.md` 新增第12节把画板动作协议写清楚，再动代码（`Agents.md` 第2条"协议优先"）。
- **"谁能画"的范围拍板**：`FULLREADME.md` 第9节对 Phase 3 的描述只说"画板引擎"（工具栏/协议/同步/撤销重做），没提权限。按 `Agents.md` 第5条"不确定的需求先问"，把这个开放假设明确写进了第12.1节和第10节审查表 #21，并在 README/PR 里标注等待用户确认：**本 Phase 房间内任意在线玩家都能画/撤销/清空，没有"仅作画者可操作"的限制**，Phase 4/5 接入真实对局流程时会在这层协议上加"当前是否轮到你画"的校验，不需要改这里定义的事件形状。
- **画板状态建模为"动作日志"而不是"笔迹列表"**：受 README"画板与回放"一节启发，把 `lineErase`（线擦）也做成一条可撤销/重做的动作（指向被擦的目标动作 id，而不是直接从列表物理删除），这样"撤销线擦=恢复那条笔迹、重做线擦=再次擦掉"是同一套撤销/重做栈自然处理的，不需要给"擦除"单独写一套逻辑。`clear`（清空）则设计成全局硬重置、不可撤销，且会清空所有玩家的撤销/重做栈——不这么做的话，清空后还能"重做"出清空前的笔迹，逻辑上说不通。细节见 `FULLREADME.md` 第12.1/12.2节。
- **实时预览 vs 落地动作分开**：画的过程中用 `canvas:strokeProgress`（无 ack、不落日志）广播实时预览，让其他客户端能看到"正在画的这一笔"；只有松手（`canvas:strokeEnd`）才真正落一条 `stroke` 动作、写进日志、参与撤销/重做。这样"多端实时同步"和"矢量协议只存有意义的东西"两个要求不冲突。
- **油漆桶设计成"服务端只存意图，客户端泛洪填充"**：`fill` 动作只存一个点+颜色，具体的像素填充效果由客户端在自己的画布位图上用泛洪填充算法（`frontend/src/canvas/floodFill.js`，栈实现+颜色容差兼容描边抗锯齿边缘）重新计算——这样服务端不用感知像素，回放时也只要在正确的时间点重新执行一次泛洪填充即可复现效果，不用额外存结果位图。
- **线擦（lineEraser）命中检测放在客户端**：客户端拖动线擦时用点到线段距离公式（`frontend/src/canvas/hitTest.js`）在本地判断碰到了哪条已渲染的笔迹，命中一条就单独提交一次 `canvas:eraseStroke`（而不是整段拖拽结束后批量提交一个"擦除区域"），这样每条被擦的笔迹都能被独立撤销/重做，粒度更细，用户体验也更符合直觉（撤销只恢复最后擦掉的那一条，不是整个拖拽动作）。服务端不重复做碰撞检测，只做"目标是否存在且当前可见"的校验。
- **渲染策略：整幅重绘，不做局部优化**：任何会改变可见动作集合的事件（新增/撤销/重做/清空/线擦命中）都触发前端 `CanvasBoard.vue` 把整个画布清空重画一遍（`redraw()`）。本地部署/小规模场景下笔迹总量有限，用"简单且正确"换掉"复杂的脏矩形/增量渲染优化"，记在 `FULLREADME.md` 第12.4节"已知限制"，以后真的观察到性能问题再优化。
- **画板内部固定 1000×1000 参考分辨率**：不管实际屏幕多大，画布内部坐标系固定为 1000×1000，`points` 协议里 0~1 的归一化坐标就是相对这个参考分辨率换算的；响应式适配（`Agents.md` 第4条硬性要求）只改 CSS 显示尺寸（`aspect-ratio:1/1` + 移动端把左侧工具栏切换成底部横向滚动栏），不改内部坐标系和 `width`（笔刷粗细）字段的含义。这样笔刷粗细在不同设备上视觉比例一致，不用做设备相关的换算。
- **测试入口**：Phase 4/5 的正式游戏页面还没做，没法把画板嵌进真实对局流程里联调。按 `Agents.md` 第5条的精神，在房间大厅页加了一个明确标注为"测试页"的入口 `/room/:id/canvas-test`（`frontend/src/pages/CanvasTest.vue`），单纯用来验证多端实时同步；正式游戏页面搭建时直接复用 `CanvasBoard.vue` 组件，不用重写画板逻辑。这个测试入口本身不是需求的一部分，是本 Phase 为了能测试而做的临时脚手架，PR 描述里会说明这一点。
- **安全自查**（画板协议涉及高频用户输入，按 `Agents.md` 规范加做一轮）：`canvas:strokeProgress` 没有限流/节流，理论上恶意客户端可以绕过前端高频发这个事件刷带宽——评估后判断本地部署/小规模场景风险有限，且这个事件转发开销小（不落日志、不用完整校验），记为已知取舍（第10节 #20），不是遗漏。上面提到的"任意玩家可画"权限假设也一并记进了审查表（#21），虽然它本质是设计范围问题而不是传统意义的"安全漏洞"，但因为涉及"谁能操作房间共享状态"，一起记录方便追踪。
- **测试方式**：延续 Phase 1/2 的判断，没有引入自动化测试框架。写了一个独立的 Node 脚本（`socket.io-client` + `axios`，不在仓库里）用两个模拟客户端跑通了完整协议：实时预览转发、落笔广播（含 payload 字段校验：非法颜色/越界坐标都应被拒绝）、油漆桶广播、线擦命中+双重擦除应被拒绝、撤销/重做（含"撤销线擦=恢复笔迹"和"重做线擦=再次擦除"两条链路）、撤销栈为空时的 no-op 行为、清空（含清空后重做栈失效验证）、非房间成员访问画板协议应被 `NOT_IN_ROOM` 拒绝——全部手工验证通过。前端额外跑了 `vite build` 和 `vite dev` 确认没有编译期错误；开发过程中发现并修了一个"取色器切回上一个工具"记错工具的小 bug（`CanvasBoard.vue` 的 `selectTool` 函数）。
