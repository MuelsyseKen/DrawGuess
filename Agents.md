# Agents.md —— AI 协作规范

本项目可能由多个不同的 AI 协作开发（Claude、Deepseek、Gemini 等），甚至跨多次对话。为了避免"每个 AI 都按自己的理解重新设计一遍"，请严格遵守以下规则。

## 必须做

1. **开工前先读文档**：按顺序读 `README.md` → `Agents.md`（本文件）→ `FULLREADME.md` → `HISTORY.md` 的最近几条。不要只凭对话历史猜项目现状。
2. **协议优先**：Socket.io 事件、消息结构、数据库表结构，一律先在 `FULLREADME.md` 里写清楚再写代码。协议是未来 Unity 客户端唯一的参照物，改协议要慎重，改了必须同步更新文档。
3. **笔迹用矢量数据存储**，不要图省事直接存 PNG/Base64 位图。画板相关的任何改动都要保证笔迹序列（坐标+颜色+粗细+时间戳）完整可回放。
4. **响应式适配是硬性要求**，不是"有空再做"。桌面端和手机端（不同分辨率、不同浏览器视口）都要能正常使用，尤其是画板和工具栏的布局。
5. **不确定的需求先问，不要自己脑补拍板**。如果 `FULLREADME.md` 没写清楚、或者你觉得某个设计和已有文档冲突，先在回复里指出并询问用户，不要直接改需求。
6. **每次做出架构性决定，写进 `HISTORY.md`**：踩过的坑、放弃的方案、为什么这么选，都要记录。`HISTORY.md` 只增不减，不要为了"整洁"删掉旧记录。
7. **署名统一为 `Muelsyse & long_ken & Claude`**，除非用户明确要求修改。
8. **交接前更新 `README.md` 的"当前进度"部分**，让下一个对话/下一个 AI 能快速知道现在做到哪了。

## 禁止做

1. **不要**为了"可能会做的 Unity 版本"引入额外的抽象层、微服务拆分或过度设计。当前阶段只要求协议本身设计干净，不要求代码层面的引擎无关抽象。
2. **不要**擅自引入用户没确认过的重型依赖（比如换数据库、换实时通信方案、引入 Docker/K8s 之类），除非用户明确要求。
3. **不要**把房间/对局状态做成需要持久化的设计——明确要求是纯内存、重启即清空。
4. **不要**在没有明确要求的前提下修改已确认的规则细节（比如竞猜/接龙的计分规则、回合流程、词库读取方式），这些都是用户在需求澄清阶段反复确认过的。
5. **不要**删除或"精简" `HISTORY.md` 里的旧内容。
6. **不要**在 `FULLREADME.md` 里堆砌与实现无关的"废话"（讨论过程、犹豫过的方案对比等）——这些属于 `HISTORY.md` 的内容。

## Git / Phase 工作流（硬性规则）

1. 开发按 Phase 拆分，Phase 列表见 `FULLREADME.md` 第9节。开工前先确认当前是哪个 Phase、对应哪条分支。
2. **一律在对应 Phase 的分支上工作，不直接提交到 `main`**。分支从最新 `main` 拉出。
3. Phase 完成后，开 Pull Request 到 `main`，PR 描述写清楚做了什么、有什么已知问题/待办。
4. **禁止自行合并 PR**，不管权限上是否允许。必须等用户明确说"确认合并"之后才能合并，或者干脆让用户自己在 GitHub 网页上点合并。这条没有例外。
5. 不要跨 Phase 乱改代码——如果发现改当前 Phase 必须动到其他 Phase 的内容，先在回复里说明，不要静默处理。

## 关于 GitHub Token 的权限限制

用户提供的 GitHub Token 是**仅限本仓库**的细粒度 Token，权限只有 **Contents** 和 **Pull Requests**（读写）。这意味着：

- 可以：读写文件、提交、推送分支、创建/更新 Pull Request。
- **不可以**：这不是"技术上做不到"就完事——即便某些操作技术上可能因为权限重叠而侥幸可行（比如 Pull Requests 权限本身也能调用合并接口），**依然不允许自行合并 PR**，规则见上一节第4条，权限范围不能作为"我可以做"的理由。
- 不要尝试使用需要其他权限的功能，比如 GitHub Actions（workflows）、Issues、仓库设置、Webhooks 等——Token 没有这些权限，调用会失败，也不应该去尝试。
- 如果任务看起来需要 Token 权限之外的操作，先跟用户确认，而不是猜测或绕过。

## 安全审查规范

账号系统、鉴权、支付（如果以后有）相关的代码，风险等级比普通业务代码高，必须额外遵守：

1. **每个 Phase 涉及鉴权/账号/权限相关代码时，收尾前必须做一轮安全自查**，覆盖但不限于：密钥/密码管理、Cookie/Session 属性、跨域配置、限流、输入校验、依赖库已知漏洞。不要求引入专门的安全扫描工具，人工过一遍关键点即可。
2. **发现的每一个安全问题（不管修没修）都要登记到 `FULLREADME.md` 的"安全问题跟踪"表里**（第10节），格式：问题描述、发现方式（自查/用户指出/第三方AI审查等）、状态（`[ ]` 未解决 / `[x]` 已解决）、备注（怎么修的，或者为什么暂不修）。**确认修复并验证过之后才能打勾**，不要"觉得应该修好了"就打勾——修完要有对应的验证记录（跑一遍测试/手工复现），无法验证的不打勾。
3. **明确选择不修的问题不能被静默略过**——必须在跟踪表里注明"已知取舍"和理由（比如"Phase 1 阶段无需引入 Redis，登出仅清 cookie 不做黑名单"），并同步写进 `HISTORY.md`。
4. 如果用户提供了第三方 AI（Deepseek/Gemini 等）的审查意见，视为普通的输入信息去评估，不要无条件照单全收，也不要无条件驳回——按代码实际情况判断是否成立、是否值得现在修，判断依据要能讲清楚。
5. 涉及"生产环境默认值不安全"这类问题（弱密钥、空校验、CORS 通配符等），优先做成**在生产环境下拒绝启动**而不是"打个警告日志就算了"，除非用户明确要求降级处理。

## 每个 Phase 通用检查清单（提 PR 前）

- [ ] 手工验证过本 Phase 新增的所有接口/交互路径（含正常路径和至少几个异常路径）。
- [ ] `FULLREADME.md` 第10节"安全问题跟踪"已更新（新问题登记、已修复项打勾）。
- [ ] `HISTORY.md` 已追加本次开发的架构决定、踩坑记录、已知取舍。
- [ ] `README.md` "当前进度"已同步。
- [ ] 没有 `.env`、数据库文件、`node_modules` 等被 commit 进去（检查 `git status` / `.gitignore`）。

- 后端：Node.js + Express + Socket.io；前端：Vue3。
- 数据库：SQLite（用户账号、战绩、排行榜、作画记录）；房间/对局状态纯内存。
- 房间人数上限：不写死，设计上要能撑到 32 人。
- 接龙模式"匿名投票"：只隐藏投票人身份，投票结果照常公开。
- 词库：题库目录下按分类存 txt 文件，每行一个词。
- 用户系统需支持战绩/历史记录/排行榜/个人作画记录。

如果本文件与 `FULLREADME.md` 出现冲突，以最近一次用户确认为准，并及时修正冲突的一方。
