# 你猜我画 —— 前端

Vue3 + Vite。详细的项目介绍、Phase 划分、页面结构见仓库根目录的 `README.md` 与 `FULLREADME.md`。

## 本地开发

```bash
cp .env.example .env
npm install
npm run dev
```

默认通过 `VITE_API_BASE_URL`（见 `.env.example`）连接后端 REST 接口。
